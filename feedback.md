# Feedback log

The daily quiz agent reads this file (see PROMPT.md step 1b) before writing new questions. Each entry with `Status: open` means: work ~2 reinforcement questions on that topic into the next questionnaire (different code example/angle than the original, not a verbatim repeat), then flip that entry to `Status: reinforced on <date>`.

## Entries

### 2026-09-07 -- re: Day 1, Q3 (NumPy array creation & dtypes)
User feedback: struggled with this question.
Status: open
